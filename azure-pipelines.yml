trigger:
- main  # Trigger on push to the main branch, change to your branch name if different

pool:
  name: myUbuntuAgentPool  # Ensure the agent is correctly registered in this pool

variables:
- group: azureLogin # Azure DevOps Service Connection for Azure
- name: azureSubscription   # Azure DevOps Service Connection name
  value: 'MyAzureServiceConnection'
- name: resourceGroup
  value: 'OzResourceGroup'
- name: location
  value: 'eastus'
- name: sshServiceConnection
  value: 'MyVM_SSH_Connection'
- name: vmUsername
  value: 'adminUser'
- name: privateKey
  value: '~/.ssh/id_ed25519'
- name: vmName
  value: 'MyLinuxVM'
- name: subscriptionId
  value: 'b5692eb3-36bc-44f3-bead-3bb1b09d4117'

jobs:
# - job: DeployResources
#   displayName: 'Deploy Resources (Network, Storage, and VM)'
#   steps:

#   - task: AzureCLI@2
#     displayName: 'Register Microsoft.Storage Provider'
#     inputs:
#       azureSubscription: $(azureSubscription)
#       scriptType: 'bash'
#       scriptLocation: 'inlineScript'
#       inlineScript: |
#         az provider register --namespace Microsoft.Storage
#         az provider show --namespace Microsoft.Storage --query "registrationState"

#   - task: AzureCLI@2
#     displayName: 'Deploy Network Resources'
#     inputs:
#       azureSubscription: $(azureSubscription)
#       scriptType: bash
#       scriptLocation: inlineScript
#       inlineScript: |
#         az deployment group create \
#           --resource-group $(resourceGroup) \
#           --template-file network-setup.json \
#           --parameters @network-setup-parameters.json

#   - task: AzureCLI@2
#     displayName: 'Deploy Virtual Machine'
#     inputs:
#       azureSubscription: $(azureSubscription)
#       scriptType: bash
#       scriptLocation: inlineScript
#       inlineScript: |
#         az deployment group create \
#           --resource-group $(resourceGroup) \
#           --template-file linux-vm.json \
#           --parameters @linux-vm-parameters.json \
#           --parameters sshPublicKey="$(cat ~/.ssh/id_ed25519.pub)"

#   - task: AzureCLI@2
#     displayName: 'Deploy Storage Account'
#     inputs:
#       azureSubscription: $(azureSubscription)
#       scriptType: bash
#       scriptLocation: inlineScript
#       inlineScript: |
#         az deployment group create \
#           --resource-group $(resourceGroup) \
#           --template-file storage-accounts.json \
#           --parameters @storage-accounts-parameters.json \

- job: BlobMigration
  # dependsOn: DeployResources
  displayName: 'Blob Migration Job'
  steps:
    # - task: AzureCLI@2
    #   displayName: 'Copy blob_migration.ps1 to VM'
    #   inputs:
    #     azureSubscription: $(azureSubscription)
    #     scriptType: bash
    #     scriptLocation: inlineScript
    #     inlineScript: |
    #       # Define VM details and the local script path
    #       echo "resourceGroup: $(resourceGroup) vmName: $(vmName) vmUsername: $(vmUsername)"
    #       echo "Fetching VM IP..."
    #       VM_IP=$(az vm show -d -g $(resourceGroup) -n $(vmName) --query "publicIps" -o tsv)
    #       echo "VM_IP: $VM_IP"

    #       if [ -z "$VM_IP" ]; then
    #         echo "ERROR: Failed to retrieve VM public IP."
    #         exit 1
    #       fi

    #       LOCAL_SCRIPT_PATH="$(Build.SourcesDirectory)/blob_migration.ps1"
    #       REMOTE_PATH="/home/$(vmUsername)/blob_migration.ps1"

    #       # Copy the script to the VM using SCP
    #       scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -v -i ~/.ssh/id_ed25519 "$LOCAL_SCRIPT_PATH" $(vmUsername)@$VM_IP:"$REMOTE_PATH"
    #       # scp -v -i ~/.ssh/id_ed25519 $LOCAL_SCRIPT_PATH $(vmUsername)@$VM_IP:$REMOTE_PATH

    # - task: AzureCLI@2
    #   displayName: 'Enable a Public DNS Name for VM'
    #   inputs:
    #     azureSubscription: $(azureSubscription)
    #     scriptType: bash
    #     scriptLocation: inlineScript
    #     inlineScript: |
    #       # Define VM details
    #       echo "resourceGroup: $(resourceGroup) vmName: $(vmName)"
    #       az network public-ip update \
    #       --resource-group $(resourceGroup) \
    #       --name MyPublicIP \
    #       --dns-name mylinuxvm-dns

    - task: AzureCLI@2
      displayName: 'Give permissions '
      inputs:
        azureSubscription: $(azureSubscription)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          # Define variables
          STORAGE_ACCOUNTS=("ozstorageaccounta" "ozstorageaccountb")

          echo "Giving the principal the Storage Blob Data Contributor role..."
          # Capture the principal ID
          # principal_ID=$(az account show --query user.name -o tsv)
          # principal_ID="718484a1-f311-4498-b889-21b735209d33"
          principal_ID="6f3fa905-ebcf-4c1b-8bda-29c03412a289"
          echo "Principal ID: $principal_ID"

          # Assign the Contributor role at the subscription level
          echo "Assigning the Contributor role at the subscription level..."
          az role assignment create --assignee $principal_ID --role Contributor --scope /subscriptions/b5692eb3-36bc-44f3-bead-3bb1b09d4117
          # az role assignment create --assignee 6f3fa905-ebcf-4c1b-8bda-29c03412a289 --role "User Access Administrator" --scope /subscriptions/b5692eb3-36bc-44f3-bead-3bb1b09d4117
          # az role assignment create --assignee $principal_ID --role Contributor --scope /subscriptions/$(subscriptionId)

          for STORAGE_ACCOUNT in "${STORAGE_ACCOUNTS[@]}"; do
            # Define the scope using the correct format
            SCOPE="/subscriptions/$(subscriptionId)/resourceGroups/$(resourceGroup)/providers/Microsoft.Storage/storageAccounts/$STORAGE_ACCOUNT"
            echo "Scope: $SCOPE"  # Debugging line to print scope

            echo "🔹 Assigning role to $STORAGE_ACCOUNT..."
            az role assignment create --assignee "$principal_ID" --role "Storage Blob Data Contributor" --scope "$SCOPE" || { echo "Failed to assign role to $STORAGE_ACCOUNT"; exit 1; }
            
          done

    - task: SSH@0
      displayName: 'Run PowerShell script on VM'
      inputs:
        sshEndpoint: $(sshServiceConnection) # Azure DevOps Service Connection for SSH (you'll configure this)
        runOptions: 'inline'
        inline: |
          # Ensure PowerShell Core and Azure CLI are installed
          export DEBIAN_FRONTEND=noninteractive

          echo "Checking if PowerShell is installed..."
          if ! command -v pwsh &> /dev/null; then
            echo "PowerShell not found. Installing..."
            sudo apt-get update -qq
            sudo apt-get install -yq wget apt-transport-https software-properties-common
            wget -q "https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/packages-microsoft-prod.deb"
            sudo dpkg -i packages-microsoft-prod.deb
            sudo apt-get update -qq
            sudo DEBIAN_FRONTEND="noninteractive" apt-get install -yq -y powershell
          else
            echo "✅ PowerShell is already installed. Skipping installation."
          fi

          echo "Checking if Azure CLI is installed..."
          if ! command -v az &> /dev/null; then
            echo "Azure CLI not found. Installing..."
            sudo apt-get update -qq
            sudo apt-get install -yq azure-cli
          else
            echo "✅ Azure CLI is already installed. Skipping installation."
          fi

          echo "Checking if Microsoft signing key is installed..."
          if [ ! -f "/usr/share/keyrings/microsoft-archive-keyring.gpg" ]; then
            echo "Signing key not found. Installing..."
            curl -sL https://packages.microsoft.com/keys/microsoft.asc | sudo -H gpg --no-tty --batch --dearmor -o /usr/share/keyrings/microsoft-archive-keyring.gpg
          else
            echo "✅ Microsoft signing key is already installed. Skipping."
          fi

          echo "Checking if Azure CLI repo is already added..."
          if ! grep -q "https://packages.microsoft.com/repos/azure-cli/" /etc/apt/sources.list.d/azure-cli.list 2>/dev/null; then
            echo "Azure CLI repo not found. Adding..."
            echo "deb [signed-by=/usr/share/keyrings/microsoft-archive-keyring.gpg] https://packages.microsoft.com/repos/azure-cli/ $(lsb_release -cs) main" \
                | sudo tee /etc/apt/sources.list.d/azure-cli.list
          else
            echo "✅ Azure CLI repo already exists. Skipping."
          fi

          echo "Updating and upgrading Azure CLI..."
          sudo apt-get update -qq
          sudo apt-get install -y azure-cli

          echo "Logging in to Azure CLI..."
          az login --service-principal -u "$(AZURE_APP_ID)" -p "$(AZURE_PASSWORD)" --tenant "$(AZURE_TENANT_ID)"

          echo "Changing file permissions to make the script executable..."
          sudo chmod +x /home/$(vmUsername)/blob_migration.ps1

          echo "Running the PowerShell script..."
          pwsh /home/$(vmUsername)/blob_migration.ps1




          # # Ensure PowerShell Core is installed
          # export DEBIAN_FRONTEND=noninteractive
          # echo "Installing PowerShell Core..."          
          # sudo apt-get update -qq
          # sudo apt-get install -yq azure-cli -y wget apt-transport-https software-properties-common
          # wget -q "https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/packages-microsoft-prod.deb"
          # sudo dpkg -i packages-microsoft-prod.deb
          # sudo apt-get update -qq
          # sudo apt-get install -yq azure-cli -y powershell
          
          # # Download and install the Microsoft signing key
          # echo "Downloading and installing the Microsoft signing key..."
          # export DEBIAN_FRONTEND=noninteractive
          # sudo rm -f /usr/share/keyrings/microsoft-archive-keyring.gpg
          # echo "Starting to install the Microsoft signing key..."
          # curl -sL https://packages.microsoft.com/keys/microsoft.asc | sudo -H gpg --no-tty --batch --dearmor -o /usr/share/keyrings/microsoft-archive-keyring.gpg

          # echo "Signing key installed."

          # # Add the Azure CLI repository
          # echo "Adding the Azure CLI repository..."
          # echo "deb [signed-by=/usr/share/keyrings/microsoft-archive-keyring.gpg] https://packages.microsoft.com/repos/azure-cli/ $(lsb_release -cs) main" \
          #     | sudo tee /etc/apt/sources.list.d/azure-cli.list

          # # Update package lists and install Azure CLI
          # echo "Updating package lists and installing Azure CLI..."
          # sudo apt-get update -y
          # sudo apt-get install -y --only-upgrade azure-cli

          # echo "Logging in to Azure CLI..."
          # echo "Azure App ID: $(AZURE_APP_ID)"
          # echo "Azure Tenant ID: $(AZURE_TENANT_ID)"
          # echo "Azure Client Password: $(AZURE_PASSWORD)"
          # az login --service-principal -u "$(AZURE_APP_ID)" -p "$(AZURE_PASSWORD)" --tenant "$(AZURE_TENANT_ID)"
 
          # # Change file permissions to make it executable
          # echo "Changing file permissions to make it executable..."
          # sudo chmod +x /home/$(vmUsername)/blob_migration.ps1

          # # Run the PowerShell script
          # echo "Running the PowerShell script..."
          # pwsh /home/$(vmUsername)/blob_migration.ps1

